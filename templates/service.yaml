apiVersion: v1
kind: Service
metadata:
  name: {{ include "medchek-app.fullname" . }}-svc
spec:
  selector:
    app: {{ include "medchek-app.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
